import React from 'react';
import { render } from '@testing-library/react';
import { UserProfileExample } from './user-profile.composition';

it('should render', () => {
  // const { getByTestId } = render(<UserProfileExample />);
  // const rendered = getByTestId('test-user-profile-page');
  // expect(rendered).toBeTruthy();
  expect(true).toBe(true);
});
